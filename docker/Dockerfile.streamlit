# Use Python slim base
FROM python:3.12-slim

# Set working directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    libpq-dev gcc && \
    rm -rf /var/lib/apt/lists/*

# Copy only requirements first (for build caching)
COPY ../requirements/ ./requirements/
# Install Streamlit + base deps
RUN pip install --no-cache-dir -r requirements/streamlit.txt

# Copy project source code
COPY ../src ./src

ENV PYTHONPATH=/app


# Expose Streamlit port
EXPOSE 8501

# Default command to run Streamlit app
CMD ["streamlit", "run", "src/cv_ui/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
